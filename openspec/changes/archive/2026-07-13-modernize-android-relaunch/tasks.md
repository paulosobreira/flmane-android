## 1. Pre-flight checks

- [x] 1.1 Verify `https://sowbreira-26fe1.firebaseapp.com/f1mane/host` is reachable over HTTPS and returns the expected plaintext host string (e.g. via `curl`); if not, flag as a blocker for section 6 before proceeding. — Confirmed: `HTTP/2 200`.
- [x] 1.2 Confirm the Firebase project has a Web Client ID valid for Credential Manager's `GetGoogleIdOption` (Firebase console → Authentication → Sign-in method → Google), matching `google-services.json`'s `default_web_client_id`. — Confirmed present (`oauth_client` with `client_type: 3`).
- [x] 1.3 Confirm ViewBinding is in scope for the Kotlin conversion (open question in design.md) before section 5 starts. — Decision: in scope.

## 2. Toolchain upgrade (still Java, existing support libs)

- [x] 2.1 Bump `gradle/wrapper/gradle-wrapper.properties` to Gradle 8.11+ — used **9.6.1** (latest; required for JDK 25 daemon support, which only landed in Gradle 9.1.0).
- [x] 2.2 Bump root `build.gradle`: AGP `com.android.tools.build:gradle` 3.3.2 → **9.2.0**, `com.google.gms:google-services` → **4.5.0**.
- [x] 2.3 Replace `jcenter()` with `mavenCentral()` in root `build.gradle`'s `buildscript.repositories` and `allprojects.repositories` (keep `google()`).
- [x] 2.4 In `app/build.gradle`, bump `compileSdkVersion`/`targetSdkVersion` to 36 and `minSdkVersion` to 24. Also added mandatory `namespace` (AGP 8+ requirement) and removed the now-conflicting `package` attribute from `AndroidManifest.xml`.
- [x] 2.5 Ensure `sdkmanager` has the `platforms;android-36` package installed locally (already present) and confirm the build-tools version AGP 9.2.0 selects is installed. — Confirmed present.
- [x] 2.6 Run `./gradlew assembleDebug` and resolve any remaining build-system-level errors. Confirm green build. — **Green.** Fixed along the way: `proguard-android.txt` → `proguard-android-optimize.txt` (no longer supported), added `android:exported="true"` to `LoginActivity` (AGP 12+ manifest requirement), bumped `jp.wasabeef:picasso-transformations` 2.1.2 → 2.4.0 (2.1.2 only ever existed on the now-dead jcenter; 2.4.0 is the only version on Maven Central — final replacement happens in section 4 anyway), switched `app/build.gradle` to `=` assignment syntax (Gradle 10 deprecation).

## 3. AndroidX + Material 3 migration (still Java)

- [x] 3.1 Replace `com.android.support:appcompat-v7`, `com.android.support:design`, `com.android.support.constraint:constraint-layout` in `app/build.gradle` with `androidx.appcompat:appcompat:1.7.1`, `com.google.android.material:material:1.14.0`, `androidx.constraintlayout:constraintlayout:2.2.1`.
- [x] 3.2 Update all `android.support.v7.app.AppCompatActivity` (and any other `android.support.*`) imports across `LoginActivity.java`, `MainActivity.java`, `GridActivity.java`, `NomeActivity.java`, `PerfilActivity.java` to their `androidx.*` equivalents.
- [x] 3.3 Update `app/src/main/res/values/styles.xml` to extend a Material 3 theme (`Theme.Material3.*`) instead of the old AppCompat/support theme.
- [x] 3.4 Update layout XML files for `android.support.*` widget namespaces — only `activity_main.xml` referenced `android.support.constraint.ConstraintLayout` directly; updated to `androidx.constraintlayout.widget.ConstraintLayout`. Others had no support-lib-qualified widget classes.
- [x] 3.5 Run `./gradlew assembleDebug`, confirm green build. **Deviation from plan**: hit a `checkDebugDuplicateClasses` failure — `play-services-auth:16.0.1` and `firebase-auth:16.2.0`/`firebase-core:16.0.7` (2019-era) still transitively pulled in ancient `com.android.support:support-compat`, conflicting with the new `androidx.core`. Fixed at the root (not via Jetifier, per design.md) by moving Firebase to BoM `34.16.0` (dropping unused `firebase-core`, which nothing in the codebase referenced) and bumping `play-services-auth` to `21.6.0` (real current versions, confirmed against `dl.google.com/android/maven2` metadata) — both are natively AndroidX already. Build is green.
  - ⚠️ **Manual smoke-test not performed**: no emulator/device is available in this environment (`emulator -list-avds` / `adb devices` both empty). Visual verification that the login screen renders correctly is deferred to section 7 and must be done by a human with a device/emulator.

## 4. Library swaps (still Java, one at a time)

- [x] 4.1 Add `com.squareup.okhttp3:okhttp:5.4.0` and `org.jetbrains.kotlinx:kotlinx-coroutines-android:1.11.0` dependencies (Kotlin stdlib/coroutines added now even though conversion happens in section 5).
- [x] 4.2 Replace `AsyncHttpClient`/`JsonHttpResponseHandler` calls in `LoginActivity.java` (`entrarAnonimo`, `entrarAutenticado`, and the photo-existence check in `preencheFotoNomeUsuario`) with OkHttp `Request`/`Callback`, keeping manual `org.json` parsing. `carregaHost()` was left on plain `java.net.URL` — it's not an AsyncHttpClient call, and its HTTP handling is rewritten in section 6 anyway. Notes: (1) OkHttp callbacks run on a background thread, unlike android-async-http's main-thread callbacks — all UI/SharedPreferences updates now go through `runOnUiThread`. (2) `entrarAutenticado`'s headers (`nome`, `urlFoto`) can contain non-ASCII bytes (accented names, URLs); switched to `Headers.Builder.addUnsafeNonAscii` because OkHttp's normal header validation rejects non-ASCII values that the old Apache-based client allowed through — a plain swap would have silently broken Google sign-in for any user with an accented display name.
- [x] 4.3 Remove `com.loopj.android:android-async-http` dependency (done as part of 4.1's edit once no references remained).
- [ ] 4.4 Manually verify guest login (`criarSessaoVisitante` / `renovarSessaoVisitante`) still works end-to-end against the real backend. — **Not performed**: no emulator/device available in this environment. Needs a human pass.
- [~] 4.5 **Deferred to section 5.** Attempted Coil 3.5.0 (`io.coil-kt.coil3:coil` + `coil-network-okhttp`) while still in Java; Coil 3's `ImageRequest.Builder` API leans heavily on Kotlin extension functions (e.g. `target(imageView)`) that don't call cleanly from Java and would need rewriting again once each file converts to Kotlin anyway. Reverted to Picasso for now; the swap now happens per-file alongside 5.4/5.9/5.7 (`ImageAdapter.kt`, `LoginActivity.kt`, `PerfilActivity.kt`), where Coil's idiomatic `imageView.load(url) { ... }` API applies directly.
- [~] 4.6 **Deferred to section 5** — see 4.5. Picasso/`picasso-transformations` removed from `app/build.gradle` once the Kotlin files no longer reference them.
- [ ] 4.7 Manually verify avatar loading in `GridActivity`'s grid, the login screen portrait, and the profile screen portrait. — Deferred until after the Coil swap lands in section 5; also needs a device/emulator (none available here).
- [x] 4.8 Add `androidx.credentials:credentials:1.6.0`, `androidx.credentials:credentials-play-services-auth:1.6.0`, and `com.google.android.libraries.identity.googleid:googleid:1.2.0` dependencies; removed `com.google.android.gms:play-services-auth`.
- [x] 4.9 Replaced `GoogleSignInClient`/`GoogleSignInOptions`/`startActivityForResult` in `LoginActivity.java` with `CredentialManager.getCredentialAsync` + `GetGoogleIdOption` (Java-friendly async API, not the suspend one — matches section 4's "still Java" scope), per `specs/google-sign-in/spec.md`. Removed `onActivityResult`/`RC_SIGN_IN` entirely.
- [x] 4.10 Updated sign-out in `PerfilActivity.java` to call `credentialManager.clearCredentialStateAsync` (fire-and-forget, same pattern as the old `mGoogleSignInClient.signOut()` it replaces) in addition to `FirebaseAuth.signOut()`, per the sign-out requirement in `specs/google-sign-in/spec.md`.
- [ ] 4.11 Manually verify: first-time Google sign-in (account picker shown), returning-user auto sign-in, sign-in cancellation, and sign-out followed by a fresh sign-in (previous account is not auto-selected). — **Not performed**: no emulator/device available in this environment. Needs a human pass; also needs the Firebase console Web Client ID check from 1.2 to hold up in practice.

## 5. Kotlin conversion

- [x] 5.1 Kotlin toolchain: added `classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:2.4.0'` in root `build.gradle`. **Deviation**: did NOT `apply plugin: 'org.jetbrains.kotlin.android'` in `app/build.gradle` — AGP 9.0+ has built-in Kotlin support and its plugin actively *rejects* the separate Kotlin Android plugin being applied ("no longer required... will be removed in a future AGP version"). The classpath dependency alone is enough to pin the effective KGP version to 2.4.0 (confirmed via `./gradlew :app:dependencies` — `kotlin-stdlib:2.4.0` wins resolution over AGP's built-in 2.2.10 floor). Also could not use the old `android.kotlinOptions { jvmTarget }` DSL (that block is contributed by the plugin that's no longer applied) — JVM target is set solely via `compileOptions.sourceCompatibility/targetCompatibility = VERSION_17`, which built-in Kotlin support picks up.
- [x] 5.2 Enabled ViewBinding (`buildFeatures { viewBinding true }`) in `app/build.gradle`, per the 1.3 decision.
- [x] 5.3 Converted `WebAppInterface.java` → `WebAppInterface.kt`.
- [x] 5.4 Converted `ImageAdapter.java` → `ImageAdapter.kt`, using Coil 3 (`imageView.load(url) { placeholder(...); transformations(CircleCropTransformation()) }`) instead of Picasso — confirms the Coil-in-Kotlin approach compiles cleanly where Coil-in-Java (attempted in section 4) did not.
- [x] 5.5 Converted `NomeActivity.java` → `NomeActivity.kt` with ViewBinding (`ActivityNomeBinding`).
- [x] 5.6 Converted `GridActivity.java` → `GridActivity.kt` with ViewBinding (`ActivityGridBinding`).
- [x] 5.7 Converted `PerfilActivity.java` → `PerfilActivity.kt` with ViewBinding + Coil. Kept `CredentialManager.clearCredentialStateAsync` (Java-style callback) rather than the suspend `clearCredentialState`, since a coroutine-based rewrite of sign-out wasn't in this task's scope and it works correctly as-is.
- [x] 5.8 Converted `MainActivity.java` → `MainActivity.kt` with ViewBinding (`ActivityMainBinding`).
- [x] 5.9 Converted `LoginActivity.java` → `LoginActivity.kt` with ViewBinding + Coil. `entrarAnonimo`/`entrarAutenticado` are now `suspend fun`s built on a private `Call.await()` extension (`suspendCancellableCoroutine` wrapping OkHttp's `enqueue`) launched via `lifecycleScope.launch` — no more manual `runOnUiThread` needed, since resuming a coroutine automatically hops back to the launching context (`Main`). Also converted `carregaHost()`'s threading (raw `Thread` → `lifecycleScope.launch(Dispatchers.IO)`), and `signIn()` to use `CredentialManager`'s suspend `getCredential` (dropped the Java-style `getCredentialAsync` from section 4 for consistency with the rest of the now-fully-coroutine file). `carregaHost()`'s actual HTTP scheme/validation logic is untouched here — that's section 6.
- [x] 5.10 `./gradlew assembleDebug` green; confirmed zero `.java` files remain under `app/src/main/java/` (`ls` shows 7 `.kt` files). Removed the now-unused Picasso/`picasso-transformations` dependencies from `app/build.gradle`.

## 6. Host discovery response validation

- [x] 6.1 **Corrected during implementation**: `carregaHost()` already fetched `https://sowbreira-26fe1.firebaseapp.com/f1mane/host` — confirmed by re-reading the pre-migration code (`git show master:...LoginActivity.java`). An earlier draft of this proposal/design/spec incorrectly claimed it was plaintext HTTP (a misreading during the initial explore phase, not an actual finding); corrected across `proposal.md`, `design.md`, and the capability spec, which was renamed `secure-backend-discovery` → `resilient-host-discovery` to stop implying a security fix that isn't real. No scheme change was made because none was needed.
- [x] 6.2 Added validation in `LoginActivity.kt`'s `carregaHost()`: the fetched value is trimmed and checked non-blank with an `https` URI scheme (`java.net.URI(...).scheme`) before being persisted to `SharedPreferences`; otherwise the previously cached `host` value is left untouched (logged via `Log.w`), per `specs/resilient-host-discovery/spec.md`.
- [ ] 6.3 Manually verify: normal host discovery on launch, and that a simulated empty/failed discovery response does not clear or corrupt a previously working cached host. — **Not performed**: no emulator/device available in this environment. Needs a human pass.

## 7. Final regression pass

**None of 7.1–7.8 could be performed in this environment** — no Android emulator or physical device is available here (`emulator -list-avds` and `adb devices` both come back empty). Everything below was verified at the level a build/compile pass can verify (compiles, resolves, matches spec'd shape); none of it has been exercised at runtime. This is a hard gap, not a formality — please run these manually (e.g. via Android Studio) before shipping:

- [ ] 7.1 Guest login end-to-end (fresh install, no cached token).
- [ ] 7.2 Guest session renewal (relaunch with a cached token).
- [ ] 7.3 Google sign-in end-to-end, including backend session creation and profile display.
- [ ] 7.4 Sign-out and re-sign-in.
- [ ] 7.5 Profile name edit (`NomeActivity`) and avatar change (`GridActivity`/`ImageAdapter`).
- [ ] 7.6 WebView loads the backend HTML5 app with the correct host/token query params.
- [ ] 7.7 JS bridge: `Android.showToast()` and `Android.exitApp()` from the web page.
- [ ] 7.8 Back-button behavior from `MainActivity` returns to `LoginActivity` as before.
- [x] 7.9 Confirmed final `minSdk`=24 / `compileSdk`=36 / `targetSdk`=36 in `app/build.gradle` match design.md's goals. Dependency list cross-checked against `app/build.gradle`: all planned removals (`com.android.support:*`, `play-services-auth` direct dep, `android-async-http`, Picasso/`picasso-transformations`) confirmed gone; all planned additions present. `proposal.md`'s "Dependencies added" list corrected to match the actual final artifact coordinates (`io.coil-kt.coil3:coil`, not `io.coil-kt:coil`; added `credentials-play-services-auth` and `lifecycle-runtime-ktx`, which were needed but not originally listed).
